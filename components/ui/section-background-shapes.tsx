"use client";

interface SectionBackgroundShapesProps {
    variant?: "default" | "minimal" | "dense";
    className?: string;
}

export function SectionBackgroundShapes({ 
    variant = "default",
    className 
}: SectionBackgroundShapesProps) {
    return null;
}
